# Global Height Texture Rules

1. Replace per-band texture grouping with a unified texture rule list.
2. Let each texture define its own weight over normalized height.
3. Keep legacy band configuration as fallback during migration.
4. Migrate the global terrain painter prefab to the new unified rule model.
